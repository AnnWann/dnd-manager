name: Apply casting warning and damage bonuses

on:
  pull_request:
    paths:
      - ".github/subtle-warning-damage-trigger"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply casting warning and damage bonus updates
        run: python .github/apply_subtle_warning_damage_bonuses.py

      - name: Remove temporary implementation files
        run: rm -f .github/subtle-warning-damage-trigger .github/apply_subtle_warning_damage_bonuses.py

      - run: npm install --package-lock=false --ignore-scripts
      - run: npm run build

      - name: Commit implementation to PR branch
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refine casting warning and damage bonuses"
          git push origin HEAD:${{ github.head_ref }}
