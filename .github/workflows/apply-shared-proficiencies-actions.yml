name: Apply shared proficiencies and minimal actions

on:
  pull_request:
    paths:
      - ".github/shared-proficiencies-actions-trigger"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply shared editors and action reference
        run: python .github/apply-shared-proficiencies-actions.py

      - name: Remove temporary implementation files
        run: rm -f .github/shared-proficiencies-actions-trigger .github/apply-shared-proficiencies-actions.py

      - run: npm install --package-lock=false --ignore-scripts
      - run: npm run build

      - name: Commit implementation to PR branch
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "share proficiency editor and add simplified actions"
          git push origin HEAD:${{ github.head_ref }}
