name: Apply versatile weapon grip

on:
  pull_request:
    paths:
      - ".github/versatile-weapon-trigger"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply versatile weapon support
        run: python .github/apply_versatile_weapon_grip.py

      - name: Remove temporary implementation files
        run: rm -f .github/versatile-weapon-trigger .github/apply_versatile_weapon_grip.py

      - run: npm install --package-lock=false --ignore-scripts
      - run: npm run build

      - name: Commit implementation to PR branch
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "add versatile weapon grip and damage"
          git push origin HEAD:${{ github.head_ref }}
